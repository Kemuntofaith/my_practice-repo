#include <stdio.h>
#include <stdlib.h>
#include "heady.h"

//extern int count;
int main()
{
	int value;
	//int result;
	
	value = increment(void);
	value = increment(void);
	value = increment(void);
	printf("value : %d\n", value);
	//count = count + value;
	//result = count + value;
	//printf("the result of count is: %d\n", count);
	//printf("the result of count is: %d\n", result);
	return 0;
}
